
TODO:


Mouse input:

	* Pathfinding to clicked tile (ignore monsters).
	* Set background of path tiles.
	* Move along path (once per turn).
	* Cancel path if user enters direction manually (or sets a different path).

UI:

	* Widget to show cycle / turn progress.
	* Show player queued action (highlight the relevant square background?).
